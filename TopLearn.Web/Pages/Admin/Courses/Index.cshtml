@page
@model TopLearn.Web.Pages.Admin.Courses.IndexModel
@{

}
@using TapLearn.Core.DTOs.Course
<body>

    <div id="wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header"> لیست دوره ها</h1>
            </div>

        </div>

        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                       لیست دوره های سایت
                    </div>
                    <div class="panel-body">
                        @*<div class="panel-body">
                            <div class="col-md-12" style="margin: 10px 0;">
                                <a asp-page="CreateCourse" class="btn btn-warning btn-lg">افزودن دوره جدید</a>
                            </div>
                        </div>*@
                        <div class="panel-body">
                            <div class="table-responsive">
                                <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline" role="grid">
                        
                                    <div class="col-md-12" style="margin: 10px 0;">
                                        <a asp-page="CreateCourse" class="btn btn-warning btn-lg">افزودن دوره جدید</a>
                                    </div>
                                    <table class="table table-striped table-bordered table-hover dataTable no-footer" id="dataTables-example" aria-describedby="dataTables-example_info">
                                        <thead>
                                            <tr>
                                                <th>تصویر </th>
                                                <th>عنوان دوره </th>
                                                <th>تعدادجلسات </th>
                                                
                                                <th>دستورات</th>
                                            <tr>

                                        </thead>
                                        <tbody>
                                             @foreach (var item in Model.showCourse)
                                              {
                                               <tr class="gradeA even">
                                                   <td>
                     
                                                           <img src="/Course/thumb/@item.ImageName" class="thumbnail" style="max-width: 120px" />
           
                                                   </td>
                                                   
                                                 <td class=" ">@item.Title</td>
                                                 <td class=" ">@item.EpisodeCount</td>
                                                
                                                 <td>
                                                     <a asp-page="EditCourse" asp-route-id="@item.Courseid" class="btn btn-warning btn-sm">ویرایش</a>
                                                     <a asp-page="IndexEpisode" asp-route-id="@item.Courseid" class="btn btn-warning btn-sm">افزودن بخش </a>

                                                     @*<a asp-page="ListDeleteUser" asp-route-id="@item.Userid" class="btn btn-danger btn-sm">حذف</a>*@
                                                 </td>

                                               </tr>

                                              }
                                        </tbody>
                                    </table>
                                    
                                </div>
                            </div>

                        </div>





                    </div>
                </div> 
            </div>
        </div>
        
     </div>
</body>